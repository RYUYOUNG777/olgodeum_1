name: motion_recognition_app
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.10.0  # 최신 안정 버전 사용
  cloud_firestore: ^5.6.1  # firebase_core와 호환되는 최신 버전
  firebase_auth: ^5.4.0
  google_sign_in: ^6.2.0
  camera: ^0.10.0     # 카메라 권한
  permission_handler: ^10.0.0
  cupertino_icons: ^1.0.8
  path_provider: ^2.0.11
  webview_flutter: ^4.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
